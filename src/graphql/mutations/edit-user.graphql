mutation EditUser($input: EditUserInput!) {
  editUser(input: $input) {
    __typename
    success
    user {
      __typename
      id
      name
      email
      phone
      role
      address
      city
      state
      country
      zipcode
    }
  }
}
